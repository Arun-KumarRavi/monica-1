FROM nginx:alpine

# Copy custom Nginx configuration optimized for EspoCRM
COPY default.conf /etc/nginx/conf.d/default.conf

WORKDIR /var/www/html

# Copy application files (to be served directly by Nginx where applicable)
COPY . /var/www/html
